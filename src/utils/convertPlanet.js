/* eslint-disable object-curly-newline */
/* eslint-disable camelcase */

export const convertPlanet = ({ name, diameter, climate, population, terrain }) => ({
  name,
  diameter,
  climate,
  population,
  terrain,
});
